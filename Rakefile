# encoding: utf-8
require 'rake'

# imported tasks will be loaded once Rakefile has been processed
spec = Gem::Specification.find_by_name 'mdotasia-theme'
import "#{spec.gem_dir}/lib/tasks/build.rake"
import "#{spec.gem_dir}/lib/tasks/validate.rake"
